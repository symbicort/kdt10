<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('./component/head', {title:'home'}) %>

</head>
<body>
    <%- include('./component/header')%>
    <h1>hello ejs</h1>
    <% for(var i = 0; i < 5; i++){ %>
        Hello World
        <% } %>

    <% for(let btn of btns) { %>
        <button type="button" class="btn btn-info"><%= btn  %></button>
    <% } %>

    <% if(!isLogin) { %>
        <h4>login not yet</h4>
        <% } else { %>
        <h4>login success</h4>
        <% } %>
    
    <h4><%= me.msg %> <%= me.name %></h4>
    
</body>
</html>